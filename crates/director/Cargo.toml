[package]
name = "director"
version = "0.1.0"
edition = "2024"

[dependencies]
uniffi = "0.27"
serde_json = "1"
serde = { version = "1.0", features = ["derive"] }

[lib]
crate-type = ["cdylib", "staticlib", "lib"]
